# Frontend env (Vite exposes only VITE_* vars to the client)
# In dev the /api proxy handles this; the absolute URL is the prod fallback.
VITE_API_URL=/api
VITE_SITE_NAME=Autos del Camino
VITE_SITE_URL=http://localhost:5471
